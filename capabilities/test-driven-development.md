# Test-Driven Development

For changed behavior, write or identify the smallest failing contract first. Confirm the failure is meaningful, implement only enough to pass, then refactor without weakening assertions. Avoid tests that merely restate implementation details.
