# Secure Review

Review trust boundaries, authorization, secret handling, injection surfaces, dependencies, and unsafe defaults. Prioritize exploitable findings with file references. Do not print secret values or run active attacks against external systems.
