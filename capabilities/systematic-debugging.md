# Systematic Debugging

Reproduce first. Capture the exact failure, trace data flow to the earliest incorrect state, form one testable hypothesis, make the smallest root-cause fix, and rerun the reproducer plus adjacent regression tests. Never stack speculative fixes.
