# Test Discipline

Reproduce failures before fixing them. Add focused regression coverage, then run the repository's existing test, lint, typecheck, and build commands that apply. Never claim a check ran unless its command completed.
